# 部署迁移记录（2026-08-27）

## 背景
原部署方案：Railway（后端） + Vercel（前端），均为海外平台，国内访问较慢。
目标：迁移到国内访问更快的方案。

---

## 最终方案：Railway（后端） + Netlify（前端）

### 为什么选这个组合？
- 后端继续用 Railway（已部署好，配置齐全，不用重新折腾）
- 前端从 Vercel 迁到 Netlify（国内访问速度比 Vercel 快）
- Cloudflare Pages 新版控制台入口不清晰，折腾未果，改用 Netlify

### 部署信息

| 项目 | 平台 | 地址 |
|------|------|------|
| 后端 API | Railway | `https://scheduling-system-production-a681.up.railway.app` |
| 前端 | Netlify | 初始：`luxury-mandazi-6acbb1.netlify.app`（可改名） |

### 关键配置

#### 后端（Railway）
- 构建方式：Dockerfile（项目根目录）
- 端口：8000
- 环境变量：
  - `SECRET_KEY`：JWT 签名密钥（必须修改）
  - `DEFAULT_ADMIN_PASS`：管理员密码
  - `CORS_ORIGINS`：允许的前端域名（可选，建议配置）
- 数据库：当前使用 SQLite（文件数据库，容器重建数据会丢失）

#### 前端（Netlify）
- 仓库：`shoemorw-16648310/scheduling-system`
- 构建配置：
  - Base directory：`frontend`
  - Build command：`npm run build`
  - Publish directory：`dist`
- 环境变量：
  - `VITE_API_BASE_URL` = `https://scheduling-system-production-a681.up.railway.app/api`
- SPA 路由重写：`frontend/public/_redirects` 文件（`/* /index.html 200`）

---

## 部署踩坑记录

### 1. Zeabur 入口变化
- 原以为 Zeabur 有免费 PaaS 部署，实际新版 Zeabur 的 "Create New Project" 直接跳到购买服务器页面
- 结论：Zeabur 可能已取消免费 PaaS，或入口变更，暂不采用

### 2. Cloudflare Pages 新版控制台
- Cloudflare 新版把 Pages 和 Workers 合并为 "Workers & Pages"
- 从 Create application 进去默认是 Worker 模式，不是 Pages 静态站点模式
- 尝试配置未果，改用 Netlify
- 如后续想迁回 Cloudflare Pages，需从 Pages 专属入口进入

### 3. Netlify 首次部署踩坑
- 第一次部署 Netlify 自动检测失败，把整个仓库根目录当静态文件上传了
- 原因：没有手动指定 Base directory 和 Build command
- 解决：手动配置 Base directory = `frontend`，Build command = `npm run build`，Publish directory = `dist`
- 配置修改后需要点 "Deploy project without cache" 重新构建

### 4. 预览地址 vs 生产地址
- Netlify 每次部署会生成带 hash 的预览地址（如 `6a8f9b14--xxx.netlify.app`）
- 预览地址可能 404，要访问正式生产地址（如 `xxx.netlify.app`）
- 点 "Open production deploy" 按钮打开的是正式地址

---

## 平台费用说明

### Railway
- 新用户 $5 免费额度（一次性，按用量扣，不是按时间）
- 后端服务（最低配）：约 $2.5-$4/月
- + PostgreSQL：再加 $3-$5/月
- 纯后端 SQLite：$5 能用 1.5-2 个月
- 后端 + PostgreSQL：$5 能用 2-3 周
- 付费起步：Hobby 计划 $5/月起

### Netlify
- Free 计划：免费，每月 100GB 带宽，300 构建分钟
- 个人使用完全够用

---

## 安全性说明

### 已有的安全措施
- JWT 登录认证
- HTTPS 传输加密
- 环境变量存储敏感信息

### 建议加固
1. 修改默认管理员密码（强密码）
2. 配置 CORS（只允许前端域名访问）
3. 切换到 PostgreSQL（数据不丢失）
4. 给所有 API 加权限校验（中优先级）

---

## 国内部署方案对比（备查）

| 方案 | 月费参考 | 性能 | 备案 | 适合场景 |
|------|---------|------|------|---------|
| 阿里云/腾讯云轻量服务器 | ¥30-60/月（新用户首年更便宜） | ⭐⭐⭐⭐ | 需要 | 长期使用、追求性能 |
| Zeabur（国内节点） | ¥30-50/月 | ⭐⭐⭐ | 不需要 | 图省事、不想备案 |
| 腾讯云 CloudBase | ¥0-30/月 | ⭐⭐⭐ | 需要 | 轻量应用 |

### 备案流程（国内服务器需要）
1. 准备材料：身份证、域名证书、服务器凭证、实名手机号
2. 云厂商控制台提交备案申请
3. 云厂商初审（1-3天）
4. 短信核验 + 人脸核验
5. 管局审核（7-15天）
6. 备案通过，拿到备案号

> 个人备案不能用于经营性网站；备案期间网站不能开放访问；换厂商需要转备案。

---

## 后续可选优化项

- [ ] 修改 Netlify 站点名称（改成好记的，如 `scheduling-system.netlify.app`）
- [ ] 配置后端 CORS_ORIGINS 环境变量
- [ ] 切换到 PostgreSQL 数据库（数据持久化）
- [ ] 绑定自定义域名
- [ ] 修改默认管理员密码为强密码
